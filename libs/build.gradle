apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('nineoldandroids-library-2.4.0.jar')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
